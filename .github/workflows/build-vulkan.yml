name: Build Ollama Vulkan Only

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo docker system prune -af --volumes

      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ollama/ollama
          ref: main

      - name: Lowercase Username
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Modify Dockerfile
        run: |
          cat > modify.py << 'EOF'
          with open('Dockerfile', 'r') as f:
              lines = f.readlines()

          output = []
          i = 0
          skip = False
          in_cpu = False
          in_build_stage = False

          while i < len(lines):
              line = lines[i]

              # === base-amd64: replace ROCm image with ubuntu + vulkan-sdk ===
              if 'FROM --platform=linux/amd64 rocm/dev-almalinux' in line and 'AS base-amd64' in line:
                  output.append('FROM --platform=linux/amd64 ubuntu:24.04 AS base-amd64\n')
                  i += 1
                  while i < len(lines) and not lines[i].startswith('FROM'):
                      i += 1
                  output.append('RUN for i in 1 2 3; do apt-get update --allow-releaseinfo-change -o Acquire::Check-Valid-Until=false && break || sleep 5; done && \\\n')
                  output.append('    apt-get install -y --no-install-recommends git build-essential gcc g++ curl wget ccache ca-certificates gnupg software-properties-common rsync python3 ninja-build pkg-config && \\\n')
                  output.append('    wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | tee /etc/apt/trusted.gpg.d/lunarg.asc && \\\n')
                  output.append('    wget -qO /etc/apt/sources.list.d/lunarg-vulkan-noble.list https://packages.lunarg.com/vulkan/lunarg-vulkan-noble.list && \\\n')
                  output.append('    for i in 1 2 3; do apt-get update && apt-get install -y vulkan-sdk && break || sleep 5; done && \\\n')
                  output.append('    rm -rf /var/lib/apt/lists/*\n')
                  continue

              # === Track build stage to strip mlx-related lines ===
              if line.startswith('FROM') and 'AS build' in line:
                  in_build_stage = True
              elif in_build_stage and line.startswith('FROM'):
                  in_build_stage = False

              if in_build_stage:
                  # Skip mlx-c git clone and mlx CGO_CFLAGS
                  if 'mlx-c' in line or ('CGO_CFLAGS' in line and 'mlx' in line):
                      i += 1
                      continue
                  # Remove -tags mlx from go build
                  if 'go build' in line and '-tags mlx' in line:
                      line = line.replace('-tags mlx ', '').replace('-tags mlx', '')

              # === cpu stage: strip dnf/gcc-toolset (base is now ubuntu) ===
              if line.startswith('FROM') and 'AS cpu' in line:
                  in_cpu = True
              elif in_cpu and line.startswith('FROM'):
                  in_cpu = False

              if in_cpu and (line.startswith('RUN dnf') or ('ENV PATH=' in line and 'gcc-toolset' in line)):
                  i += 1
                  continue

              # === Replace unneeded stages (cuda/rocm/jetpack/mlx) with scratch ===
              if 'FROM' in line and (('AS cuda-' in line) or 'AS rocm-6' in line or ('jetpack' in line) or 'AS mlx' in line):
                  stage = line.split('AS')[1].strip()
                  output.append(f'FROM scratch AS {stage}\n')
                  skip = True
                  i += 1
                  continue

              if skip:
                  if line.startswith('FROM'):
                      skip = False
                  else:
                      i += 1
                      continue

              # === Comment out COPY from unneeded stages ===
              if line.startswith('COPY --from=cuda-') or line.startswith('COPY --from=jetpack-') or line.startswith('COPY --from=rocm-6') or line.startswith('COPY --from=mlx'):
                  output.append('# ' + line)
                  i += 1
                  continue

              output.append(line)
              i += 1

          with open('Dockerfile', 'w') as f:
              f.writelines(output)
          EOF
          
          python3 modify.py

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.OWNER_LC }}/ollama-vulkan
          tags: type=raw,value=latest

      - name: Build
        uses: docker/build-push-action@v5
        timeout-minutes: 240
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        if: success()
        run: echo "✅ ghcr.io/${{ env.OWNER_LC }}/ollama-vulkan:latest" >> $GITHUB_STEP_SUMMARY
